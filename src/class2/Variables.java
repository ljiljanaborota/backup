package class2;

public class Variables {
    /*
    below line is reserving a box in the computer memory.
    we are calling it box1 if we need information that we are storing inside the box
     we can simple say to the computer  give us the contents of box 1.
     int=> what type of data we want to store
     box1=> is the name of the box
     = sign will take whatever we write after it and will store it inside the box1

     */
    public static void main(String[] args) {
    int box1=10;
        System.out.println(box1);

        short myBox=10000;
        System.out.println(myBox);



    }
}
